'use client';

// The AI Support chat is rendered by the dashboard layout when pathname === '/dashboard/chat'.
// This page component is intentionally empty — the layout handles expanded chat rendering.
export default function ChatPage() {
  return null;
}
